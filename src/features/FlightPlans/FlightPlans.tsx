import React, { FC } from 'react';

import { Page } from '../../common/components/Page/Page';

interface FlightPlansProps {};

export const FlightPlans: FC<FlightPlansProps> = () => (
    <Page title="Flight plans" className="FlightPlans">
    </Page>
);
